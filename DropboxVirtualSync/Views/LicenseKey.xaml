<Window x:Class="DropboxVirtualSync.Views.LicenseKey"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:DropboxVirtualSync.Views"
      mc:Ignorable="d"
      Title="LicenseKey" Height="100" Width="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBox Grid.Row="1" Grid.Column="0" Margin="5 0 0 0"></TextBox>
        <Button 
            x:Name="UseLicenseKeyButton" 
            Grid.Row="1" 
            Grid.Column="1" 
            Content="Use Key" 
            Margin="0 0 5 0"
            Click="UseLicenseKeyButton_OnClick" />
    </Grid>
</Window>
