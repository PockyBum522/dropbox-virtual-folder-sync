<Window x:Class="DropboxVirtualSync.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        
        <!-- Grid -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        
        <!-- Buttons -->
        <Button x:Name="MirrorStartButton" Click="MirrorStartButton_OnClick" Grid.Row="0" Grid.Column="1" Height="50" Width="100" Content="Start Mirror"/>
        <Button x:Name="MirrorStopButton" Click="MirrorStopButton_OnClick" Grid.Row="1" Grid.Column="1" Height="50" Width="100" Content="Stop Mirror"/>
        
        <!-- Textboxen -->
        <TextBox x:Name="SourcePathTextBox" Grid.Row="0" Grid.Column="0"></TextBox>
        <TextBox x:Name="DestinationPathTextBox" Grid.Row="1" Grid.Column="0"></TextBox>
        
    </Grid>
</Window>
